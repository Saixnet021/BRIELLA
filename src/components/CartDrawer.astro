---
// src/components/CartDrawer.astro
---

<div id="cartDrawerBackdrop" class="fixed inset-0 z-50 bg-briella-charcoal/60 backdrop-blur-sm opacity-0 pointer-events-none transition-all duration-300" role="dialog" aria-modal="true" aria-hidden="true">
  <div id="cartDrawerPanel" class="fixed top-0 right-0 bottom-0 w-full max-w-md bg-white border-l border-briella-cream-dark shadow-2xl flex flex-col justify-between transform translate-x-full transition-transform duration-300">
    
    <!-- Drawer Header -->
    <div class="p-4 sm:p-5 border-b border-briella-cream-dark flex items-center justify-between bg-briella-cream-light">
      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-briella-blue stroke-[1.5]" viewBox="0 0 24 24" fill="none" stroke="currentColor">
          <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path>
          <line x1="3" y1="6" x2="21" y2="6"></line>
          <path d="M16 10a4 4 0 0 1-8 0"></path>
        </svg>
        <h2 class="font-serif text-lg sm:text-xl font-medium text-briella-charcoal">Mi Pedido Briella</h2>
      </div>

      <button id="btnCartClose" class="w-8 h-8 rounded-full bg-white border border-briella-cream-dark flex items-center justify-center text-briella-charcoal hover:bg-briella-charcoal hover:text-white transition-colors" aria-label="Cerrar carrito">
        <svg class="w-4 h-4 stroke-[1.5]" viewBox="0 0 24 24" fill="none" stroke="currentColor">
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>
    </div>

    <!-- Drawer Items List -->
    <div id="cartItemsList" class="flex-1 overflow-y-auto p-4 sm:p-5 space-y-4">
      <!-- Injected by Client Script -->
    </div>

    <!-- Drawer Footer / Checkout Area -->
    <div id="cartFooterArea" class="hidden p-4 sm:p-6 border-t border-briella-cream-dark bg-briella-cream space-y-3.5">
      
      <!-- Subtotal Row -->
      <div class="flex items-baseline justify-between">
        <span class="text-xs font-bold uppercase tracking-widest text-briella-charcoal/70">Subtotal Estimado:</span>
        <span id="cartSubtotalValue" class="text-xl font-bold font-sans text-briella-charcoal">S/ 0.00</span>
      </div>

      <!-- Customer Details Form -->
      <div class="space-y-2">
        <input 
          type="text" 
          id="cartInputCustomerName" 
          placeholder="Nombre y Apellidos *" 
          required
          class="w-full bg-white text-xs py-2 px-3 border border-briella-cream-dark focus:border-briella-blue outline-none"
        />
        <div class="grid grid-cols-2 gap-2">
          <input 
            type="text" 
            id="cartInputCustomerDni" 
            placeholder="DNI / Documento *" 
            maxlength="12"
            required
            class="w-full bg-white text-xs py-2 px-3 border border-briella-cream-dark focus:border-briella-blue outline-none"
          />
          <input 
            type="text" 
            id="cartInputCustomerCity" 
            placeholder="Ciudad / Distrito *" 
            required
            class="w-full bg-white text-xs py-2 px-3 border border-briella-cream-dark focus:border-briella-blue outline-none"
          />
        </div>
        <input 
          type="text" 
          id="cartInputCustomerAddress" 
          placeholder="Dirección de Entrega *" 
          required
          class="w-full bg-white text-xs py-2 px-3 border border-briella-cream-dark focus:border-briella-blue outline-none"
        />
      </div>

      <p id="cartFormError" class="hidden text-[11px] text-red-600 font-medium">
        Por favor ingresa Nombre, DNI y Dirección.
      </p>

      <!-- Send WhatsApp Order Button -->
      <button 
        id="btnWhatsAppCheckoutSubmit" 
        type="button"
        class="w-full bg-briella-whatsapp hover:bg-briella-whatsapp-dark text-white py-3.5 px-4 text-xs font-bold uppercase tracking-widest transition-all shadow-wa flex items-center justify-center gap-2"
      >
        <svg class="w-4 h-4" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12.04 2C6.58 2 2.13 6.45 2.13 11.91C2.13 13.66 2.59 15.36 3.45 16.86L2.05 22L7.3 20.62C8.75 21.41 10.38 21.83 12.04 21.83C17.5 21.83 21.95 17.38 21.95 11.92C21.95 6.46 17.5 2 12.04 2M12.04 20.15C10.56 20.15 9.11 19.76 7.85 19L7.55 18.83L4.43 19.65L5.26 16.61L5.06 16.29C4.24 15 3.8 13.47 3.8 11.91C3.8 7.37 7.5 3.67 12.04 3.67C16.58 3.67 20.28 7.37 20.28 11.92C20.28 16.46 16.58 20.15 12.04 20.15M16.57 14.45C16.32 14.32 15.1 13.72 14.87 13.64C14.64 13.55 14.48 13.51 14.31 13.76C14.15 14.01 13.67 14.57 13.52 14.73C13.38 14.9 13.23 14.92 12.98 14.8C12.73 14.67 11.93 14.41 10.98 13.57C10.24 12.91 9.74 12.1 9.6 11.85C9.45 11.6 9.58 11.47 9.71 11.34C9.82 11.23 9.96 11.05 10.09 10.91C10.21 10.76 10.26 10.66 10.34 10.5C10.42 10.33 10.38 10.19 10.32 10.07C10.26 9.94 9.77 8.74 9.56 8.25C9.36 7.76 9.16 7.83 9.01 7.82C8.87 7.81 8.71 7.81 8.54 7.81C8.38 7.81 8.11 7.87 7.88 8.12C7.65 8.37 7 8.98 7 10.21C7 11.44 7.9 12.63 8.02 12.79C8.15 12.96 9.78 15.47 12.28 16.55C12.87 16.81 13.33 16.96 13.69 17.07C14.29 17.26 14.83 17.23 15.26 17.17C15.74 17.1 16.74 16.57 16.95 15.99C17.15 15.41 17.15 14.92 17.09 14.8C17.03 14.7 16.82 14.57 16.57 14.45Z"/>
        </svg>
        <span>Enviar Pedido por WhatsApp</span>
      </button>

      <p class="text-[11px] text-center text-briella-charcoal/60">
        Atención directa y confirmación de pago por WhatsApp.
      </p>

    </div>

  </div>
</div>

<script is:inline>
  window.BriellaCartClient = {
    items: [],
    waNumber: '51923340617',

    init() {
      this.load();
      this.bind();
      this.render();
    },

    load() {
      try {
        const saved = localStorage.getItem('briella_cart_astro_v1');
        if (saved) this.items = JSON.parse(saved);
      } catch (e) {
        this.items = [];
      }
    },

    save() {
      try {
        localStorage.setItem('briella_cart_astro_v1', JSON.stringify(this.items));
      } catch (e) {}
    },

    bind() {
      const trigger = document.getElementById('btnCartTrigger');
      const closeBtn = document.getElementById('btnCartClose');
      const backdrop = document.getElementById('cartDrawerBackdrop');
      const submitBtn = document.getElementById('btnWhatsAppCheckoutSubmit');

      if (trigger) trigger.addEventListener('click', () => this.open());
      if (closeBtn) closeBtn.addEventListener('click', () => this.close());
      if (backdrop) {
        backdrop.addEventListener('click', (e) => {
          if (e.target === backdrop) this.close();
        });
      }

      if (submitBtn) {
        submitBtn.addEventListener('click', () => this.sendOrder());
      }

      // Bind all card Add buttons
      document.querySelectorAll('.btn-add-to-cart').forEach(btn => {
        btn.addEventListener('click', (e) => {
          e.stopPropagation();
          const product = JSON.parse(btn.dataset.productJson || '{}');
          if (product.id) {
            this.addItem(product, 1);
          }
        });
      });
    },

    open() {
      const backdrop = document.getElementById('cartDrawerBackdrop');
      const panel = document.getElementById('cartDrawerPanel');
      if (backdrop && panel) {
        backdrop.classList.remove('opacity-0', 'pointer-events-none');
        backdrop.classList.add('opacity-100', 'pointer-events-auto');
        panel.classList.remove('translate-x-full');
        panel.classList.add('translate-x-0');
        document.body.style.overflow = 'hidden';
        this.render();
      }
    },

    close() {
      const backdrop = document.getElementById('cartDrawerBackdrop');
      const panel = document.getElementById('cartDrawerPanel');
      if (backdrop && panel) {
        backdrop.classList.add('opacity-0', 'pointer-events-none');
        backdrop.classList.remove('opacity-100', 'pointer-events-auto');
        panel.classList.add('translate-x-full');
        panel.classList.remove('translate-x-0');
        document.body.style.overflow = '';
      }
    },

    addItem(product, qty = 1, customNote = '') {
      const idx = this.items.findIndex(
        it => it.id === product.id && it.customNote === customNote
      );

      if (idx > -1) {
        this.items[idx].qty += qty;
      } else {
        this.items.push({
          id: product.id,
          nombre: product.nombre,
          material: product.material,
          precio: product.precio,
          imagen: product.imagen,
          qty,
          customNote
        });
      }

      this.save();
      this.render();
      this.showToast(`Añadido: "${product.nombre}"`);
    },

    updateQty(index, qty) {
      if (qty <= 0) {
        this.removeItem(index);
        return;
      }
      if (this.items[index]) {
        this.items[index].qty = qty;
        this.save();
        this.render();
      }
    },

    removeItem(index) {
      if (this.items[index]) {
        const removed = this.items.splice(index, 1);
        this.save();
        this.render();
        if (removed.length) {
          this.showToast(`Se eliminó "${removed[0].nombre}"`);
        }
      }
    },

    render() {
      const badge = document.getElementById('cartBadgeCount');
      const list = document.getElementById('cartItemsList');
      const footer = document.getElementById('cartFooterArea');
      const subtotalEl = document.getElementById('cartSubtotalValue');

      const totalCount = this.items.reduce((s, it) => s + it.qty, 0);
      const subtotal = this.items.reduce((s, it) => s + (it.precio * it.qty), 0);

      if (badge) badge.textContent = totalCount;
      if (subtotalEl) subtotalEl.textContent = `S/ ${subtotal.toFixed(2)}`;

      if (!list) return;

      if (this.items.length === 0) {
        list.innerHTML = `
          <div class="text-center py-12 text-briella-charcoal/60 space-y-3">
            <svg class="w-10 h-10 mx-auto text-briella-blue/40 stroke-[1.25]" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path>
              <line x1="3" y1="6" x2="21" y2="6"></line>
              <path d="M16 10a4 4 0 0 1-8 0"></path>
            </svg>
            <p class="text-xs uppercase tracking-luxury">Tu bolsa de compras está vacía</p>
          </div>
        `;
        if (footer) footer.classList.add('hidden');
        return;
      }

      if (footer) footer.classList.remove('hidden');

      list.innerHTML = this.items.map((it, idx) => `
        <div class="flex items-center gap-3 pb-3 border-b border-briella-cream-dark">
          <img src="${it.imagen}" alt="${it.nombre}" class="w-14 h-14 object-cover border border-briella-cream-dark bg-[#FAF8F5]" />
          
          <div class="flex-1 min-w-0">
            <h4 class="font-serif text-sm font-medium text-briella-charcoal truncate">${it.nombre}</h4>
            <span class="text-[10px] text-briella-blue uppercase tracking-wider block">${it.material}</span>
            ${it.customNote ? `<span class="text-[10px] bg-briella-cream-dark px-1.5 py-0.5 inline-block my-0.5 text-briella-charcoal">Grabado: "${it.customNote}"</span>` : ''}
            <div class="text-xs font-bold font-sans text-briella-charcoal">S/ ${(it.precio * it.qty).toFixed(2)}</div>
          </div>

          <div class="flex flex-col items-end gap-1.5">
            <button onclick="window.BriellaCartClient.removeItem(${idx})" class="text-xs text-briella-charcoal/40 hover:text-red-600 transition-colors" aria-label="Eliminar item">
              &times;
            </button>
            <div class="flex items-center border border-briella-cream-dark bg-white">
              <button onclick="window.BriellaCartClient.updateQty(${idx}, ${it.qty - 1})" class="w-5 h-5 flex items-center justify-center font-bold text-[10px] hover:bg-briella-cream">-</button>
              <span class="w-5 text-center text-[11px] font-bold font-sans">${it.qty}</span>
              <button onclick="window.BriellaCartClient.updateQty(${idx}, ${it.qty + 1})" class="w-5 h-5 flex items-center justify-center font-bold text-[10px] hover:bg-briella-cream">+</button>
            </div>
          </div>
        </div>
      `).join('');
    },

    sendOrder() {
      const nameInput = document.getElementById('cartInputCustomerName');
      const dniInput = document.getElementById('cartInputCustomerDni');
      const cityInput = document.getElementById('cartInputCustomerCity');
      const addressInput = document.getElementById('cartInputCustomerAddress');
      const errEl = document.getElementById('cartFormError');

      const name = nameInput ? nameInput.value.trim() : '';
      const dni = dniInput ? dniInput.value.trim() : '';
      const city = cityInput ? cityInput.value.trim() : '';
      const address = addressInput ? addressInput.value.trim() : '';

      if (!name || !dni || !address) {
        if (errEl) errEl.classList.remove('hidden');
        return;
      }

      if (errEl) errEl.classList.add('hidden');

      const subtotal = this.items.reduce((s, it) => s + (it.precio * it.qty), 0).toFixed(2);

      let msg = `*NUEVO PEDIDO - JOYERÍA BRIELLA*\n`;
      msg += `------------------------------------\n`;
      msg += `*PRODUCTOS:*\n`;

      this.items.forEach(it => {
        msg += `• ${it.qty}x *${it.nombre}* (${it.material}) - S/ ${(it.precio * it.qty).toFixed(2)}\n`;
        if (it.customNote) msg += `  └ _Detalle: "${it.customNote}"_\n`;
      });

      msg += `------------------------------------\n`;
      msg += `*Subtotal Total:* S/ ${subtotal}\n`;
      msg += `*DATOS DEL CLIENTE:*\n`;
      msg += `• *Nombre:* ${name}\n`;
      msg += `• *DNI/Doc:* ${dni}\n`;
      msg += `• *Ciudad/Distrito:* ${city || 'Por coordinar'}\n`;
      msg += `• *Dirección:* ${address}\n`;
      msg += `• *Medio de Pago:* Yape / Plin / Transferencia\n\n`;
      msg += `¡Hola Joyería Briella! Quiero confirmar este pedido y coordinar el pago/envío.`;

      const waUrl = `https://wa.me/${this.waNumber}?text=${encodeURIComponent(msg)}`;
      window.open(waUrl, '_blank');
      this.close();
    },

    showToast(message) {
      let toast = document.getElementById('briellaToastBox');
      if (!toast) {
        toast = document.createElement('div');
        toast.id = 'briellaToastBox';
        toast.className = 'fixed bottom-5 left-5 z-50 bg-briella-charcoal text-white text-xs py-2.5 px-4 rounded shadow-lg flex items-center gap-2 toast-slide-up';
        document.body.appendChild(toast);
      }
      toast.innerHTML = `
        <svg class="w-4 h-4 text-briella-whatsapp stroke-[2.5]" viewBox="0 0 24 24" fill="none" stroke="currentColor">
          <polyline points="20 6 9 17 4 12"></polyline>
        </svg>
        <span>${message}</span>
      `;
      toast.style.display = 'flex';
      setTimeout(() => {
        toast.style.display = 'none';
      }, 2500);
    }
  };

  document.addEventListener('DOMContentLoaded', () => {
    window.BriellaCartClient.init();
  });
</script>
